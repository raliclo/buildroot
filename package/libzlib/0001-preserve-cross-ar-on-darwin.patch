--- a/configure
+++ b/configure
@@ -331,1 +331,3 @@
-        if "${CROSS_PREFIX}libtool" -V 2>&1 | grep Apple > /dev/null; then
+        if test -n "${AR}"; then
+            ARFLAGS="${ARFLAGS-rc}"
+        elif "${CROSS_PREFIX}libtool" -V 2>&1 | grep Apple > /dev/null; then
@@ -338,1 +340,1 @@
-        ARFLAGS="-o" ;;
+        test -n "${ARFLAGS}" || ARFLAGS="-o" ;;
